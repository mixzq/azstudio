# AZ Studio SEO 检查报告

检查日期：2026 年 9 月 24 日。范围：线上首页、Start、Service、Projects、Contact、Ayeeyo 项目详情，以及 robots.txt 和 sitemap.xml。

## 已确认的线上状态

抽查的六个页面均返回 HTTP 200。原始 HTML 中，六个页面使用相同的标题和元描述。除首页外，另外五个页面的 canonical 都指向首页，Open Graph 信息也沿用首页。项目详情虽会在 React 加载后更新部分标签，但它与原始 HTML 的信号不一致。规范 URL 的判断依据是 [Google 对 JavaScript 网站的建议](https://developers.google.com/search/docs/crawling-indexing/javascript/javascript-seo-basics)。

robots.txt 返回 HTTP 200，未阻止抽查的公开页面。缺少显式的 robots 元标签本身不表示禁止索引。sitemap.xml 返回 HTTP 200，但只包含首页。网站地图的用途及规范参见 [Google 网站地图文档](https://developers.google.com/search/docs/crawling-indexing/sitemaps/build-sitemap)。

原始 HTML 没有页面主体或 H1，因为网站由 React 在浏览器中渲染。这不能据此断定用户或 Google 渲染后的页面没有 H1。需要使用 Search Console 的网址检查工具核对 Google 实际获取的渲染结果。Google 可以处理 JavaScript，但服务端返回有区分度的页面信息仍更稳妥。

## 本地已完成的修正

1. 首页保留独立标题、描述、规范 URL、Open Graph、Twitter 卡片以及 Organization 和 WebSite 结构化数据，增加显式的 `index, follow` 指令。
2. Start、Service、Projects、Contact 和已发布项目详情在服务端返回各自的标题、元描述、规范 URL、Open Graph、Twitter 卡片和结构化数据。项目优先使用 ACF SEO 字段，字段为空时才使用项目正文及图片作为回退值。
3. 网站地图按当前 WordPress 已发布项目动态生成，包含四个静态内页、首页及三个目前已发布的项目。CMS 内容会经短时缓存更新，无需把新的项目网址手写进 XML。
4. robots.txt 保持公开页面可抓取，限制后台与内部接口。不存在的路径返回 HTTP 404 和 `noindex` 响应头。Footer 中原本指向未实现页面的 Privacy Policy 与 Terms 链接暂时移除。

## 验证与边界

本地构建通过。用当前公开 CMS 数据测试了四个静态内页、三个项目详情、网站地图及 404 响应，页面规范 URL 与 JSON 结构化数据均通过程序检查。

这些修正尚未推送或部署，因此线上抽查结果仍代表旧版本。Vercel 实际路由和缓存行为还需在预览部署中复核。PageSpeed Insights 本次返回 HTTP 429，因此没有可靠的性能数值，也没有取得真实用户指标。未连接 Google Search Console，不能确认页面是否已被收录，也不能保证搜索排名变化。
